{
  "name": "@runebase/connector",
  "version": "0.1.0",
  "description": "DApp connector library for the Runebase MetaMask Snap",
  "repository": {
    "type": "git",
    "url": "https://github.com/Runebase/runebase-metamask-snap.git"
  },
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist && yarn build",
    "start": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "test": "jest"
  },
  "peerDependencies": {
    "@metamask/providers": ">=16.0.0"
  },
  "devDependencies": {
    "@metamask/providers": "^16.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.0"
  }
}
